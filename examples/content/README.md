# Demo Content

`examples/` 只包含演示用的虚构内容，随 `luna-site` 源码一起按 MIT 分发。

- 公开仓库 CI 与开源贡献者使用这里的 Demo 内容构建（`npm run build:demo`）。
- 正式内容永远放在私有仓库 `labuno/luna-ore`，不会进入本目录。
- 目录结构与私有内容仓库一致：`content/` + `media/`。
