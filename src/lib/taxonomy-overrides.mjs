/**
 * Stable URL mapping for categories / sections / tags / genres.
 * Format: 'stable-slug': '展示名称'
 *
 * Renaming a display name here keeps the historical URL working.
 */
/** @type {Record<string, string>} */
export const taxonomyOverrides = {
  // 'agent-notes': 'Agent 笔记',
};
